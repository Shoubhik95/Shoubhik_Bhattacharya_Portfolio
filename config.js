/**
 * Production / local API URL config.
 * - Local (npm start): API_URL empty → same origin (localhost:3000)
 * - Netlify live: build script sets API_URL to your Render backend URL
 */
window.PORTFOLIO_CONFIG = {
  API_URL: ""
};
